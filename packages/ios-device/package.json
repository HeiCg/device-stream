{
  "name": "@device-stream/ios-device",
  "version": "1.0.0",
  "description": "iOS physical device streaming via WDA MJPEG + qvh H.264 fallback",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HeiCg/device-stream.git",
    "directory": "packages/ios-device"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "device-stream",
    "ios",
    "physical-device",
    "webdriveragent",
    "mjpeg",
    "go-ios",
    "quicktime"
  ],
  "license": "MIT",
  "dependencies": {
    "@device-stream/core": "^1.0.0",
    "node-fetch": "^2.7.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/node-fetch": "^2.6.9",
    "@types/ws": "^8.5.10",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "ws": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
}
